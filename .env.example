# Required
DISCORD_BOT_TOKEN=

# Optional: HTTP dashboard/health server. Most hosts provide PORT automatically.
PORT=8080

# Optional: keep this on a persistent disk/volume on the hosting provider.
SQLITE_PATH=./data/families.sqlite

# Optional pino level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info